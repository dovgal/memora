-- Умные дистракторы: сохраняем неверные ответы учащегося.
-- Генерация практики использует их как варианты-ловушки в новых MCQ —
-- проверка становится прицельной именно там, где учащийся ошибается.
ALTER TABLE course_review_logs
  ADD COLUMN IF NOT EXISTS answer_given TEXT;
