version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: memora
    ports:
      - "5432:5432"
    volumes:
      - memora_pgdata:/var/lib/postgresql/data

volumes:
  memora_pgdata:
